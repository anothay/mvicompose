package com.rumatu.mvicompose.mvi.contracts

/**
 * Results are states that [MviAction] produces when they are performed by the processor.
 * These can be loading, successful execution, errors etc.
 */
interface MviResult
